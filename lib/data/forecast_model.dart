class Forecast {
  int id;
  DateTime date;
  double tempMin;
  double tempMax;
  int humidity;
  double windSpeed;

  Forecast(this.id, this.date, this.tempMin, this.tempMax, this.humidity,
      this.windSpeed);
}
